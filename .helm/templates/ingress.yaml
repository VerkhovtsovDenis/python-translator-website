apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.appName }}
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - http:
      paths:
      - path: /translator-website(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: {{ .Values.appName }}
            port:
              number: 80